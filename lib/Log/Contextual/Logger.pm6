role Log::Contextual::Logger;

#my $?ROLE = Log::Contextual::Logger;
#for <trace debug info warn error fatal> -> $name {
  #my $is-name = "is-$name";

  #$?ROLE.^add_method($name, method (*@_) { ... });
  #$?ROLE.^add_method($is-name, method { ... });
#}
